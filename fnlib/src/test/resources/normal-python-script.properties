cfgfns.ScriptRunner.pythonScript.script=import json\ninput_json = json.loads(input)\nrecords = input_json["records"]\nrecords["items"] = [\n    {**item, "value": item["value"] + item["value"]}\n    for item in records["items"]\n]\nrecords
cfgfns.ScriptRunner.pythonScript.scriptLanguage=PYTHON